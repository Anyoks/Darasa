%h1= @topic.name

%p= @topic.code

%p= image_tag @topic.attachment_url(:thumb), height: "300"
%p= @topic.unit.name

%p
	-if current_user && current_user.admin?
		= link_to 'Edit', edit_topic_path(@topic)
		\|
		= link_to 'Back', topics_path
